set number
syntax on
set tabstop=2
noremap <space> /

" highlight searches
set hlsearch

" ignore case in search unless capital letter is used.
set ignorecase
set smartcase

" Enalbe use of the mouse for all modes
if has('mouse')
	set mouse=a
endif
